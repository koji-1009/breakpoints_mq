name: breakpoints_mq
description: A lightweight library for implementing Responsive layout grid. Provide body, margin.
version: 3.0.0
homepage: https://github.com/koji-1009/breakpoints_mq

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
