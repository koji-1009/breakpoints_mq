name: breakpoints_mq
description: A lightweight library for implementing Responsive layout grid. Provide body, column, marging.
version: 2.1.2
homepage: https://github.com/koji-1009/breakpoints_mq

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
